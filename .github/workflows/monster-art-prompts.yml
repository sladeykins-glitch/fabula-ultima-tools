name: Add monster artwork prompts

on:
  push:
    paths:
      - 'scripts/patch-monster-art-prompts.py'
      - '.github/workflows/monster-art-prompts.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply patch
        run: python scripts/patch-monster-art-prompts.py
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: Commit changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/App.tsx src/styles.css
          if git diff --cached --quiet; then
            echo 'No source changes to commit.'
          else
            git commit -m 'Add Midjourney-ready monster artwork prompts'
            git push
          fi
